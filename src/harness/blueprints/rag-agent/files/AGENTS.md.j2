# AGENTS.md

> _Generated by [harness-kit](https://github.com/jcaiagent7143-ui/harness-kit) v{{ profile.harness_version }} · blueprint `{{ blueprint.name }}` v{{ blueprint.version }}._
> _Edit `.harness/profile.yaml` and run `harness sync` to regenerate; don't edit this file directly._

You are an AI agent working in **{{ profile.name }}** — {{ profile.description }}.

This project is configured as a **RAG agent**: every factual claim you produce
must be grounded in a retrieved chunk and cited by `chunk_id`. Hallucinated
citations fail validation.

**Project type**: `{{ profile.project_type }}` · **Language**: `{{ profile.primary_language }}` · **Frameworks**: {{ frameworks_list }}

---

## How you work in this project

1. **Ingest** — chunk source documents and embed them. See `SKILLS/chunk-and-embed/SKILL.md`.
2. **Retrieve** — for every question, retrieve top-k chunks and rerank. See `SKILLS/retrieve-and-rerank/SKILL.md`.
3. **Answer** — compose an answer that cites each claim by `chunk_id`. See `SKILLS/answer-with-citations/SKILL.md`.
4. **Verify** — run `harness verify` to confirm citations resolve and schema validates.

## Commands you may run

{% if profile.test_command -%}- Tests: `{{ profile.test_command }}`
{% endif -%}
{% if profile.lint_command -%}- Lint:  `{{ profile.lint_command }}`
{% endif -%}
{% if profile.build_command -%}- Build: `{{ profile.build_command }}`
{% endif -%}
{% if profile.dev_command -%}- Dev:   `{{ profile.dev_command }}`
{% endif %}
- Sample RAG run: `./scripts/test_task.sh "<your question>"`
- Validate output: `harness verify --json`

## Conventions

{% for c in profile.conventions -%}
- {{ c }}
{% endfor %}

## Forbidden

**Never touch** these paths:
{% for p in profile.forbidden_paths -%}
- `{{ p }}`
{% endfor %}

**Never run** these commands:
{% for c in profile.forbidden_commands -%}
- `{{ c }}`
{% endfor %}

## Recommended MCP servers

{% for m in pruned_mcps -%}
- `{{ m }}`
{% endfor %}

## Definition of done

{% for c in profile.success_criteria -%}
- {{ c }}
{% endfor %}
- Every claim in the final answer cites at least one `chunk_id` that resolves.
- `harness verify` exits 0.

## Validation contract

The `harness verify` tool runs:
{% for v in blueprint.validators -%}
- **{{ v.name }}** — {{ v.description }}
{% endfor %}

You may invoke individual checks: `harness verify --check <name>`.
